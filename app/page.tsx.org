export default function Home() {
  return (
    <main className="relative min-h-screen bg-gradient-to-b from-sky-50 via-sky-100 to-slate-100 flex flex-col items-center overflow-hidden">
      
      {/* 水面の反射エフェクト */}
      <div className="absolute inset-0 z-0 pointer-events-none">
        <div className="water-glow" />
      </div>

      {/* Hero / Logo */}
      <section className="text-center mt-20 mb-10 px-4 relative z-10">
        <h1 className="text-5xl font-light text-slate-800 tracking-wide mb-2">
          Hydro Turbine Lab
        </h1>
        <p className="text-slate-600 text-lg tracking-wide mb-6">
          水車選定シミュレーター
        </p>

        <div className="w-40 mx-auto h-1 bg-gradient-to-r from-sky-300 via-sky-400 to-sky-500 rounded-full opacity-70" />
      </section>

      {/* Input Card */}
      <div className="bg-white/60 backdrop-blur-md shadow-md border border-sky-100 rounded-2xl p-10 w-full max-w-md mx-4 relative z-10">
        <form action="/result" method="GET" className="space-y-6">
          
          {/* 流量 Q */}
          <div>
            <label className="block text-slate-700 font-medium mb-1">
              流量 Q (m³/s)
            </label>
            <input
              type="number"
              step="0.1"
              name="Q"
              required
              className="w-full border border-sky-200 rounded-lg p-3 text-slate-800 focus:outline-none focus:ring-2 focus:ring-sky-300 bg-white/70"
            />
          </div>

          {/* 落差 H */}
          <div>
            <label className="block text-slate-700 font-medium mb-1">
              落差 H (m)
            </label>
            <input
              type="number"
              step="0.1"
              name="H"
              required
              className="w-full border border-sky-200 rounded-lg p-3 text-slate-800 focus:outline-none focus:ring-2 focus:ring-sky-300 bg-white/70"
            />
          </div>

          {/* 電源周波数 */}
          <div>
            <label className="block text-slate-700 font-medium mb-1">
              電源周波数
            </label>

            <div className="flex items-center space-x-6 text-slate-700">

              <label className="flex items-center space-x-2">
                <input
                  type="radio"
                  name="freq"
                  value="50"
                  required
                  className="text-sky-500 focus:ring-sky-300"
                />
                <span>50 Hz</span>
              </label>

              <label className="flex items-center space-x-2">
                <input
                  type="radio"
                  name="freq"
                  value="60"
                  required
                  className="text-sky-500 focus:ring-sky-300"
                />
                <span>60 Hz</span>
              </label>

            </div>
          </div>

          {/* ボタン */}
          <button
            type="submit"
            className="w-full bg-sky-500 text-white py-3 rounded-lg text-lg font-medium hover:bg-sky-600 transition-colors shadow-sm"
          >
            判定する
          </button>

        </form>
      </div>

      {/* Footer */}
      <footer className="mt-20 mb-6 text-slate-500 text-xs tracking-wide relative z-10">
        © 2026 Hydro Turbine Lab
      </footer>

    </main>
  );
}